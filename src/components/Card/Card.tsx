import React, {Component} from 'react';
import './Card.scss';

export class Card extends Component {
    render() {
        return (
            <div>Card</div>
        )
    }
}

export default Card;
